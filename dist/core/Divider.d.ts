import { SFC } from 'react';
import { IDefault } from './Advertise';
interface IProps extends IDefault {
    inset?: boolean;
    light?: boolean;
}
declare const Divider: SFC<IProps>;
export default Divider;
