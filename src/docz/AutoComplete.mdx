---
name: AutoComplete
route: /autocomplete
---

import AutoComplete from '../core/AutoComplete'
import ListItem from '../core/ListItem'
import TextField from '../core/TextField'
import Typography from '../core/Typography'
import { Playground, PropsTable } from 'docz'

# AutoComplete

## Properties
<PropsTable of={ AutoComplete } />

## Simple
<Playground style={ { minHeight: '200px' } }>
    <AutoComplete
        data={ ['Rock', 'Pop', 'Classic'] }
        inputElement={
            <TextField
                fullWidth
                placeholder='Music genres'
            />
        }
        renderSuggestion={ (item, props, selected) =>
            <ListItem
                key={ item }
                selected={ selected }
                { ...props }>
                { item }
            </ListItem>
        }
    />
</Playground>

## With subheader
<Playground style={ { minHeight: '200px' } }>
    <AutoComplete
        data={ [{ label: 'Genres', subheader: true }, 'Rock', 'Pop', 'Classic'] }
        defaultValue='Rock'
        inputElement={
            <TextField
                fullWidth
                placeholder='Music genres'
            />
        }
        renderSuggestion={ (item, props, selected) => item.subheader
            ? (
                <ListItem key={ item }>
                    <Typography variant='caption'>
                        { item.label }
                    </Typography>
                </ListItem>
            )
            : (
                <ListItem
                    key={ item }
                    { ...props }
                    selected={ selected }>
                    { item.label || item }
                </ListItem>
            )
        }
    />
</Playground>
