---
name: Button
route: /button
---

import Button from '../components/Button'
import { MdKeyboardArrowLeft } from 'react-icons/md'
import { Playground, PropsTable } from 'docz'
import IconAdd from '@material-ui/icons/add'

# Button

## Properties
<PropsTable of={ Button } />

## Demo
<Playground>
    <Button margin={ 12 }>
        Default
    </Button>
    <Button color="primary" margin={ 12 }>
        Primary
    </Button>
    <Button color="secondary" margin={ 12 }>
        Secondary
    </Button>
    <Button variant="raised" color="primary" margin={ 12 }>
        Raised primary
    </Button>
    <Button variant="raised" color="secondary" margin={ 12 }>
        Raised secondary
    </Button>
    <Button variant="outlined" color="primary" margin={ 12 }>
        Outlined primary
    </Button>    
    <Button variant="outlined" color="secondary" margin={ 12 }>
        Outlined secondary
    </Button>
    <Button variant="fab" color="primary" margin={ 12 }>
        <IconAdd />
    </Button>
    <Button mini variant="fab" color="primary" margin={ 12 }>
        <IconAdd />
    </Button>
    <Button size="small" color="primary" variant="raised" margin={ 12 }>
        Small
    </Button>
    <Button size="medium" color="primary" variant="raised" margin={ 12 }>
        Medium
    </Button>
    <Button size="large" color="primary" variant="raised" margin={ 12 }>
        Large
    </Button>
    <Button disabled variant="raised" color="primary" margin={ 12 }>
        Disabled
    </Button>
</Playground>
