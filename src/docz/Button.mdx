---
name: Button
route: /button
---

import Button from '../core/Button'
import { Playground, PropsTable } from 'docz'
import { Add as IconAdd } from '../icons'

# Button

## Properties
<PropsTable of={ Button } />

## Demo
<Playground>
    <Button margin={ 12 }>
        Default
    </Button>
    <Button color='primary' margin={ 12 }>
        Primary
    </Button>
    <Button color='secondary' margin={ 12 }>
        Secondary
    </Button>
    <Button variant='contained' color='primary' margin={ 12 }>
        Contained primary
    </Button>
    <Button variant='contained' color='secondary' margin={ 12 }>
        Contained secondary
    </Button>
    <Button variant='outlined' color='primary' margin={ 12 }>
        Outlined primary
    </Button>
    <Button variant='outlined' color='secondary' margin={ 12 }>
        Outlined secondary
    </Button>
    <Button selected variant='contained' color='primary' margin={ 12 }>
        Selected
    </Button>
    <Button variant='dashed' color='primary' margin={ 12 }>
        Dashed primary
    </Button>
    <Button variant='dashed' color='secondary' margin={ 12 }>
        Dashed secondary
    </Button>
    <Button variant='fab' color='primary' margin={ 12 }>
        <IconAdd />
    </Button>
    <Button mini variant='fab' color='primary' margin={ 12 }>
        <IconAdd />
    </Button>
    <Button size='small' color='primary' variant='contained' margin={ 12 }>
        Small
    </Button>
    <Button size='medium' color='primary' variant='contained' margin={ 12 }>
        Medium
    </Button>
    <Button size='large' color='primary' variant='contained' margin={ 12 }>
        Large
    </Button>
    <Button disabled variant='contained' color='primary' margin={ 12 }>
        Disabled
    </Button>
    <input
        accept='image/*'
        style={ { display: 'none' } }
        id='flat-button-file'
        multiple
        type='file'
    />
    <label htmlFor='flat-button-file'>
        <Button variant='outlined' component='span'>
            Upload
        </Button>
    </label>
</Playground>
